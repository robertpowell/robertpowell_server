# Contact pages carry a per-page secret (the form token). Serving them uncompressed removes the
# theoretical BREACH vector that testssl flagged in the 2026-09-05 review. Applies to every vhost.
<Files "contact.php">
    SetEnv no-gzip 1
</Files>
